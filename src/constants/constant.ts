export const ListName = {
	s: "s",
};

export const ListColumns = {
	Id: "Id",
	Title: "Title",
	Date: "Date",
	Location: "Location",
	Optional: "Optional",
};

export const ListSelectColumns = [
	ListColumns.Id,
	ListColumns.Title,
	ListColumns.Date,
	ListColumns.Location,
	ListColumns.Optional,
].join(",");
